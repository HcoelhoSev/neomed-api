spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/neomed
    username: postgres
    password: admin

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true

  flyway:
    enabled: true